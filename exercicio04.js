const restaurante = {
    nome: 'Cantinho Italiano',
    tipoCozinha: 'Italiana',
    localizacao: 'Centro, São Paulo',
};

console.log(restaurante);
console.log(`${restaurante.nome} - ${restaurante.tipoCozinha}`);
console.log(`Localização: ${restaurante.localizacao}`);
